version: '3.9'
services:
  Open-Facilities:
    build:
      context: .
      dockerfile: Dockerfile
    image: open-facilities-test
    container_name: open-facilities-test
    ports:
      - "3000:3000"
    # env_file:
    #   - ../../.env
